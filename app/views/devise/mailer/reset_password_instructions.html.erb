<p><%= @resource.username %>様</p>

<p>パスワードの変更リクエストが行われました。下記のリンクから変更が可能です。</p>

<p><%= link_to 'パスワードを変更する', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>パスワードの変更リクエストをしていない場合はこのメールを無視してください。</p>
<p>上記のリンクにアクセスし、新しいパスワードを作成するまでパスワードは変更されません。</p>
